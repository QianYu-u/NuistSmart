<Page
    x:Class="NuistSmart.Views.NewsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NuistSmart.Models"
    xmlns:viewmodels="using:NuistSmart.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="校园公告" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="南信大最新动态速览" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    Command="{x:Bind ViewModel.LoadNewsCommand}"
                    Style="{StaticResource DefaultButtonStyle}"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    <TextBlock Text="刷新"/>
                </StackPanel>
            </Button>
        </Grid>

        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Categories}"
                  SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}"
                  SelectionMode="Single"
                  Margin="0,16,0,0"
                  ScrollViewer.HorizontalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                  ScrollViewer.VerticalScrollMode="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="CornerRadius" Value="16"/>
                    <Setter Property="Padding" Value="12,6"/>
                    <Setter Property="MinWidth" Value="60"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Border Grid.Row="2" Height="1" Background="{ThemeResource SurfaceStrokeColorDefaultBrush}" Margin="0,12,0,12"/>

        <Grid Grid.Row="3">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Width="50" Height="50" Canvas.ZIndex="99"/>

            <ListView ItemsSource="{x:Bind ViewModel.NewsList}"
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                      ItemClick="OnItemClick">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0,6,0,6"/>
                        <Setter Property="Margin" Value="0,0,12,0"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:NewsItem">
                        <Grid>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="8" Padding="16,12">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{x:Bind Title}" Grid.Column="0"
                                               Style="{ThemeResource BodyStrongTextBlockStyle}"
                                               TextTrimming="CharacterEllipsis" MaxLines="1"
                                               VerticalAlignment="Center" ToolTipService.ToolTip="{x:Bind Title}"/>

                                    <Border Grid.Column="1" Visibility="{x:Bind IsNew}"
                                            Background="{ThemeResource SystemAccentColor}"
                                            CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Margin="8,0,0,0">
                                        <TextBlock Text="NEW" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                    </Border>

                                    <TextBlock Text="{x:Bind Date}" Grid.Column="2"
                                               Style="{ThemeResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

                <ListView.Footer>
                    <StackPanel Padding="0,20">
                        <Button HorizontalAlignment="Center"
                                Command="{x:Bind ViewModel.LoadMoreNewsCommand}"
                                Visibility="{x:Bind ViewModel.HasMoreData, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.IsLoadingMore, Converter={StaticResource InverseBooleanConverter}, Mode=OneWay}">
                            <Grid>
                                <StackPanel Orientation="Horizontal" Spacing="8" 
                                            Visibility="{x:Bind ViewModel.IsLoadingMore, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}">
                                    <FontIcon Glyph="&#xE70D;" FontSize="14"/>
                                    <TextBlock Text="加载更多公告"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Spacing="8" 
                                            Visibility="{x:Bind ViewModel.IsLoadingMore, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                                    <ProgressRing Width="16" Height="16" IsActive="True"/>
                                    <TextBlock Text="正在加载..."/>
                                </StackPanel>
                            </Grid>
                        </Button>

                        <TextBlock Text="--- 没有更多了 ---" 
                                   HorizontalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Visibility="{x:Bind ViewModel.HasMoreData, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}"/>
                    </StackPanel>
                </ListView.Footer>

            </ListView>
        </Grid>
    </Grid>
</Page>