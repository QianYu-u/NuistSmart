<Page
    x:Class="NuistSmart.Views.BusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:NuistSmart.ViewModels"
    xmlns:models="using:NuistSmart.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 配置区域 -->
        <StackPanel Grid.Row="0" Spacing="16">
            <TextBlock Text="校园巴士自动抢票" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" />
            <TextBlock Text="选择路线和日期后，点击&quot;获取班次&quot;查看可用班车，勾选要抢的班次后点击&quot;开始抢票&quot;" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <Grid ColumnSpacing="20" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ComboBox Grid.Row="0" Grid.Column="0" Header="始发站" ItemsSource="{x:Bind ViewModel.StationList}" SelectedItem="{x:Bind ViewModel.TargetStart, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                <ComboBox Grid.Row="0" Grid.Column="1" Header="终点站" ItemsSource="{x:Bind ViewModel.StationList}" SelectedItem="{x:Bind ViewModel.TargetEnd, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                <Button Grid.Row="0" Grid.Column="2" Content="获取班次" Command="{x:Bind ViewModel.GetAvailableBusesCommand}" VerticalAlignment="Bottom" Width="120" Style="{ThemeResource AccentButtonStyle}" IsEnabled="{x:Bind ViewModel.IsNotProcessing, Mode=OneWay}">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="G" Modifiers="Control"/>
                    </Button.KeyboardAccelerators>
                </Button>

                <TextBox Grid.Row="1" Grid.Column="0" Header="目标日期 (例如：2026-04-05)" Text="{x:Bind ViewModel.TargetDate, Mode=TwoWay}" />
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Header="认证 Token (留空自动抓包)" Text="{x:Bind ViewModel.UserToken, Mode=TwoWay}" PlaceholderText="留空则尝试自动抓包，如果报错或已自行抓取，请直接填入 Token" />
            </Grid>

            <ProgressBar IsIndeterminate="True" Visibility="{x:Bind ViewModel.IsLoadingBuses, Mode=OneWay}" Margin="0,8,0,0" />
        </StackPanel>

        <!-- 班次选择区域 -->
        <StackPanel Grid.Row="1" Spacing="12" Visibility="{x:Bind ViewModel.HasBusesLoaded, Mode=OneWay}">
            <Grid>
                <TextBlock Text="可用班次 (请勾选要抢的班次)" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                    <Button Content="全选" Command="{x:Bind ViewModel.SelectAllBusesCommand}" Width="80" Height="32" FontSize="12"/>
                    <Button Content="清空" Command="{x:Bind ViewModel.ClearSelectionCommand}" Width="80" Height="32" FontSize="12"/>
                </StackPanel>
            </Grid>

            <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Padding="12" MaxHeight="200">
                <ScrollViewer VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{x:Bind ViewModel.AvailableBuses, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:BusRunDisplay">
                                <CheckBox Content="{x:Bind DisplayText, Mode=OneWay}" 
                                          IsChecked="{x:Bind IsSelected, Mode=TwoWay}" 
                                          FontFamily="Consolas" 
                                          FontSize="14" 
                                          Margin="0,4"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- 操作按钮 -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="开始抢票" Command="{x:Bind ViewModel.StartProcessCommand}" Style="{ThemeResource AccentButtonStyle}" IsEnabled="{x:Bind ViewModel.IsNotProcessing, Mode=OneWay}" Width="120">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control"/>
                    </Button.KeyboardAccelerators>
                </Button>
                <Button Content="强制停止" Command="{x:Bind ViewModel.StopProcessCommand}" IsEnabled="{x:Bind ViewModel.IsProcessing, Mode=OneWay}" Width="100" />
                <Button Content="清除日志" Command="{x:Bind ViewModel.ClearLogsCommand}" Width="100" />
            </StackPanel>

            <ProgressBar IsIndeterminate="True" Visibility="{x:Bind ViewModel.IsProcessing, Mode=OneWay}" />
        </StackPanel>

        <!-- 日志输出终端区域 -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="系统日志控制台" FontWeight="SemiBold" Margin="0,0,0,8" />
            
            <Border Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Padding="12">
                <ScrollViewer x:Name="LogScrollViewer" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{x:Bind ViewModel.Logs, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <TextBlock Text="{x:Bind}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
