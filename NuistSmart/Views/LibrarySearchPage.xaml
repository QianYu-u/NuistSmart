<Page
    x:Class="NuistSmart.Views.LibrarySearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NuistSmart.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBox Width="360"
                     PlaceholderText="请输入书名或关键词"
                     Text="{x:Bind ViewModel.SearchKeyword, Mode=TwoWay}" />
            <Button Content="搜索"
                    Command="{x:Bind ViewModel.SearchCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,12,0,12">
            <ProgressRing Width="20"
                          Height="20"
                          IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
            <TextBlock Text="正在搜索..." Opacity="0.8" />
        </StackPanel>

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.SearchResults, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:BookItem">
                    <StackPanel Margin="0,8" Spacing="4">
                        <TextBlock Text="{x:Bind Title}" TextWrapping="Wrap" Style="{ThemeResource SubtitleTextBlockStyle}" />
                        <TextBlock Text="{x:Bind Author}" Opacity="0.85" />
                        <TextBlock Text="{x:Bind Publisher}" Opacity="0.75" />
                        <TextBlock Text="{x:Bind CallNumber}" Opacity="0.75" />
                        <TextBlock Text="{x:Bind AvailableStatus}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
