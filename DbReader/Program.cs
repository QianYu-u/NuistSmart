using LiteDB; using System; using System.IO; class Program { static void Main() { var path = Directory.GetFiles(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "nuistsmart.db", SearchOption.AllDirectories); if(path.Length > 0) { using var db = new LiteDatabase(path[0]); var col = db.GetCollection("contacts"); foreach(var doc in col.FindAll()) { if(doc["Name"].AsString.Contains("ÒûÊ³")) Console.WriteLine($"{doc["Name"]} : {doc["Phone"]}"); } } } }
